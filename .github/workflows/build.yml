name: Build and Package MouseClicker

      
    
    
      
    
on:
      
    
    
      
    
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

      
    
    
      
    
jobs:
  build:
    runs-on: windows-latest

steps:
  - name: Checkout code
    uses: actions/checkout@v3

  - name: Set up .NET
    uses: actions/setup-dotnet@v3
    with:
      dotnet-version: '6.0.x'

  - name: Build project
    run: dotnet build MouseClick.csproj --configuration Release

  - name: Publish executable
    run: dotnet publish MouseClick.csproj --configuration Release --output ./publish


    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '6.0.x'

    - name: Build the project
      run: dotnet build -c Release

    - name: Publish executable
      run: dotnet publish -c Release -r win-x64 --self-contained

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: MouseClickerExecutable
        path: path/to/your/output/folder/*.exe
        
